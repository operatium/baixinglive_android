<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/baixing_live_swipe_refresh"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/baixing_live_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:padding="@dimen/dp.8"
            android:clipToPadding="false"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="2"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/baixing_live_footer_container" />

        <!-- 底部加载状态容器 -->
        <LinearLayout
            android:id="@+id/baixing_live_footer_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- 加载中提示 -->
            <ProgressBar
                android:id="@+id/baixing_live_footer_loading"
                android:layout_width="@dimen/dp.24"
                android:layout_height="@dimen/dp.24"
                android:layout_margin="@dimen/dp.8"
                android:visibility="gone" />

            <!-- 加载完毕提示 -->
            <TextView
                android:id="@+id/baixing_live_footer_end"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:padding="@dimen/dp.8"
                android:text="已经到底啦~"
                android:textSize="@dimen/sp.14"
                android:textColor="#999999"
                android:visibility="gone" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>